#include<bits/stdc++.h>
using namespace std;
int main()
{
    string str,str1;
    int n,i,j,a;
    cin>>a>>n;
    cin str;
    while(n--)
    {
        cin>>str1;
        if(str1=="pop_back")
        {
          str.pop_back();
        }
        else if(str1=="front")
        {
            str.front();
        }
        else if(str1=="back")
        {
            str.back();
        }
        else if(str1=="sort")
        {
            str.sort(str.begin(),str.end());
        }
        else if(str1=="reverse")
        {
            str.reverse(str.begin(),str.end());
        }
        else if(str1=="print")
        {
            int pos;
            pos=i;
            s
        }
        else if(str1=="reverse")
        {
            str.reverse();
        }

    }


    cout<<str;


}
